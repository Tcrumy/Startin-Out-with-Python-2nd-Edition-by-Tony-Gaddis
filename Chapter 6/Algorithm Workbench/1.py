# Write a statement that generates a random number in the range of 1 through 100
# and assigns it to a variable named rand.

# You must import the random librart to generate random numbers
import random

# Define main function
def main():

    # Here is the requested statement
    rand = random.randint(1,100)

    print(rand)

main()
